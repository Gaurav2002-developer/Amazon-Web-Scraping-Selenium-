# 🛒 Amazon Speakers Scraper

This project is a simple web scraping tool that extracts information about **speakers** from **Amazon**.  
It uses **Selenium** for dynamic content loading and **BeautifulSoup** for parsing the HTML to retrieve key product details such as:

- Product Title  
- Price  
- Product Link
- Delivery

The scraped data is stored in a CSV file (`data.csv`) for further analysis.

---

## ⚙️ Features
- Automated browsing using Selenium
- HTML parsing using BeautifulSoup
- Data stored in CSV format
- Easy to modify for other product categories

---

## 🧰 Requirements
To install dependencies, run:
```bash
pip install -r requirements.txt
